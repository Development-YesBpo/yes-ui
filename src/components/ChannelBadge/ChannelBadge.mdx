import { Meta, Canvas } from '@storybook/blocks'
import * as Stories from './ChannelBadge.stories'

<Meta of={Stories} />
# ChannelBadge
Identifica el canal de una conversación. Referencia: `preview/components-badges.html`
<Canvas of={Stories.AllChannels} />
